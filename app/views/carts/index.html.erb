
<%# breadcrumb :carts_index %>

<div class="container">
  <div class="row">
    <div class="col-xs-3">
      <%= render 'layouts/header' %>
    </div>

    <div class="col-xs-9">
      <h2 class="shop_name">Votre histoire</h2>

      <div class="space"></div>

      <div class="title_cart">CONFIRMATION</div>


      <div class="note">
        <p>下記ご注文内容で送信してもよろしいでしょうか？<br>
            よろしければ、「次へ」ボタンをクリックしてください。</p>
      </div>

      <div class="space"></div>

      <table class="table-date">
        <tr>
          <th>ご注文商品</th>
        </tr>
      </table>

      <table class="table_nomal table-bordered" width="100%">
        <tr>
          <th>商品写真</th>
          <th>商品名</th>
          <th>単価（税込）</th>
          <th>小計</th>
        </tr>

        <% @carts.each do |cart, i| %>
          <tr>
            <td width="20%"><%= attachment_image_tag cart.item, :image, size: '100x100', fallback: 'no_image.jpg' %> </td>
            <td width="40%"><%= cart.item.prodct_name %></td>
            <td width="15%"><%= cart.item.price %>円</td>
            <% subtotal = cart.item.price %>
            <td width="15%"><%= subtotal.round(0) %></td>
          </tr>
        <% end %>

        <tr>
          <th colspan="3", style="text-align:right;">小計</th>
          <td></td>
        </tr>
        <tr>
          <th colspan="3", style="text-align:right;">送料</th>
          <td>600円</td>
        </tr>
        <tr>
          <th colspan="3", style="text-align:right;">手数料</th>
          <td></td>
        </tr>
        <tr>
          <th colspan="3", style="text-align:right;">合計</th>
          <td></td>
        </tr>
      </table>

      <div class="space"></div>

      <div class="choice"">
        &emsp;お支払方法
      </div>
      <div class="method">
      </div>

      <div class="choice">
        &emsp;お届け先
      </div>
      <div class="method">
      </div>

      <div class="choice"">
        &emsp;お支払方法
      </div>
      <div class="method">
      </div>

      <%= link_to "戻る", new_user_cart_path(current_user.id)%>
      <%= link_to "次へ", complete_order_path(current_user.id) %>
    </div>
  </div>
</div>
